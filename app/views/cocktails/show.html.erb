<h2><%= @cocktail.name %></h2>
<h4>Description</h4>
<ul>
  <% @cocktail.doses.each do |dose| %>
  <li><%= dose.description %> of <%= dose.ingredient.name %><%= link_to 'Delete istruction', dose_path(dose), method: :delete, data: { confirm: "Are you sure you want to delete this dose?" } %></p></li>
  <% end %>
  <p><%= link_to 'Add something to this Cocktail recipe', new_cocktail_dose_path(@cocktail) %></p>
<p><%= link_to 'Delete this cocktail', cocktail_path(@cocktail), method: :delete, data: { confirm: "Are you sure you want to delete this cocktail?" } %></p>
<p><%= link_to 'Back to home', cocktails_path %></p>
